# Domain docs

Single-context layout.

## Location

- Domain language and context: `CONTEXT.md` at repo root
- Architectural decisions: `docs/adr/` at repo root

## Consumer rules

Skills that read these docs (`improve-codebase-architecture`, `diagnose`, `tdd`):
- Read `CONTEXT.md` first to understand domain language
- Read `docs/adr/*.md` for past decisions relevant to the task