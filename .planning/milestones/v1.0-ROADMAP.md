# Milestone v1.0: MVP

**Status:** ✅ SHIPPED 2026-04-24
**Phases:** 1-6
**Total Plans:** 17

## Overview

Initial MVP — portfolio analytics dashboard with security hardening, performance fixes, health indicators, benchmark tracking, dashboard polish, and automated tests.

## Phases

### Phase 1: Security Hardening

**Goal**: Fix critical credential exposure vulnerabilities and add API authentication before any production exposure.
**Depends on**: —
**Plans**: 2 plans

Plans:

- [x] 01-01: Remove debug endpoint, redact credentials, add auth token validation
- [x] 01-02: Add security headers, CORS policy, Docker cleanup

**Details:**
Phase 1 blocking — no production exposure until complete. BROKR_AUTH_TOKEN defaults to empty (disabled) in dev, must be set for production. CORS policy defaults to same-origin, configurable for Hermes integration.

### Phase 2: Performance

**Goal**: Fix blocking I/O in yfinance enrichment and thread safety issues in session/FX cache.
**Depends on**: —
**Plans**: 2 plans

Plans:

- [x] 02-01: Run enrich_positions() in thread pool — prevent event loop blocking
- [x] 02-02: Fix thread safety in session and FX cache

**Details:**
Use asyncio.to_thread() for non-blocking yfinance calls. Session cache (_session dict) with threading.Lock. FX rate cache with proper locking.

### Phase 3: Health Indicators

**Goal**: Add portfolio health monitoring signals for concentration risk, sector weighting, drawdown, and rebalancing.
**Depends on**: Phase 2
**Plans**: 2 plans

Plans:

- [x] 03-01: Add compute_health_alerts() — concentration, sector, drawdown, rebalancing signals
- [x] 03-02: Add health alerts UI + Hermes context integration

**Details:**
Alerts appear in dashboard UI and Hermes context. Configurable thresholds via environment variables (no hardcoding).

### Phase 4: Benchmark Tracking

**Goal**: Add S&P 500 / MSCI World benchmark comparison and historical performance tracking.
**Depends on**: Phase 2
**Plans**: 5 plans

Plans:

- [x] 04-01: Backend snapshot module + /api/benchmark endpoint + snapshot-on-refresh
- [x] 04-02: Frontend benchmark chart + attribution table UI
- [x] 04-03: Hermes context extension with benchmark + attribution data
- [x] 04-04: Gap closure — hermes-context without portfolio + empty-state messages

**Details:**
Use yfinance for benchmark data (^GSPC for S&P 500). Attribution = position_return × weight × direction.

### Phase 5: Dashboard Polish

**Goal**: Replace browser alerts with toast notifications, improve error states, fix responsive issues.
**Depends on**: Phase 4
**Plans**: 2 plans

Plans:

- [x] 05-01: Toast notification system — CSS + JS ToastManager + integration
- [x] 05-02: Error states + responsive — stale indicator, positions error state, mobile CSS

**Details:**
Toast library or simple custom implementation (no heavy dependency). Error states show last valid data with "stale" indicator.

### Phase 6: Testing

**Goal**: Add automated test coverage for core logic modules.
**Depends on**: Phase 5
**Plans**: 4 plans

Plans:

- [x] 06-01: Infrastructure — tests/ directory, conftest.py, run_tests.sh, .dockerignore
- [x] 06-02: test_scoring.py — compute_momentum_score, compute_value_score, compute_scores
- [x] 06-03: test_market_data.py — get_fx_rate, compute_rsi, enrich_position
- [x] 06-04: test_degiro_client.py — _kv_list_to_dict, fetch_portfolio

**Details:**
pytest framework, mocking yfinance and degiro-connector. CI should run tests before any phase commit.

---

## Milestone Summary

**Key Decisions:**

- DeGiro auth via intAccount + JSESSIONID (not username/password) — works reliably
- Vanilla JS frontend (no framework) — no build step, keeps Docker image small
- In-memory session cache (no database) — acceptable for single-user use case
- Hermes integration via REST API — Hermes calls Brokr, not push
- Fix security issues before adding features — critical credential exposure must be addressed first
- S&P 500 (^GSPC) as default benchmark — indexed to 100 at first snapshot date
- Attribution = (position_return - benchmark_return) × weight × direction for relative

**Issues Resolved:**

- Critical credential exposure in debug endpoint
- No API authentication on protected endpoints
- Blocking I/O in yfinance enrichment (event loop was blocked)
- Thread safety issues in session and FX cache
- Browser alert() calls blocking UX
- No automated tests for core logic

**Technical Debt Incurred:**

- DeGiro session expiry handling (no auto-reauth, documented as known issue)
- No persistent storage — session lost on restart

---

_For current project status, see .planning/ROADMAP.md_
