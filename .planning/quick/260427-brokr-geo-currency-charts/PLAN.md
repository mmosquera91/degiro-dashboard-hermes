# brokr-geo-currency-charts

Add two new doughnut charts (Geographic Exposure + Currency Exposure) to the dashboard charts section in app/static/.

- index.html: add two chart-card divs alongside existing chart cards
- app.js: add geo + currency chart logic in renderCharts()
