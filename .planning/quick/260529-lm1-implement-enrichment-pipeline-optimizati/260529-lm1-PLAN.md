---
phase: quick-260529-lm1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/market_data.py
  - app/main.py
autonomous: true
requirements: [T2, T3, T4]

must_haves:
  truths:
    - "Batch price fetch in enrich_positions uses threads=True and chunks symbol groups of >25 into batches of 25"
    - "Stage 2 symbol resolution writes the symbol cache to disk exactly once at the end of the loop, not per-symbol"
    - "/api/refresh-prices acquires _operation_lock (via the async wrapper) before running enrichment, eliminating the race with the daily loop, while still returning {\"status\": \"enrichment_started\"} immediately"
  artifacts:
    - path: "app/market_data.py"
      provides: "Chunked threaded batch download (T2) + deferred symbol-cache save (T3)"
      contains: "threads=True"
    - path: "app/main.py"
      provides: "Lock-safe fire-and-forget refresh-prices endpoint (T4)"
      contains: "asyncio.create_task"
  key_links:
    - from: "app/market_data.py enrich_positions stage 2 loop"
      to: "_save_symbol_cache"
      via: "thread-local defer flag suppresses in-loop disk writes, single explicit save after loop"
      pattern: "_save_symbol_cache"
    - from: "app/main.py refresh_prices"
      to: "_do_enrich_session_async"
      via: "asyncio.create_task fire-and-forget on the event loop"
      pattern: "create_task.*_do_enrich_session_async"
---

<objective>
Implement Fase 1 tasks T2, T3, T4 from .hermes/plans/enrichment-optimization.md to speed up the enrichment pipeline and fix a concurrency bug. Each task is a SEPARATE atomic commit.

Purpose:
- T2: Let yfinance parallelize batch price downloads internally (threads=True) while chunking to avoid Yahoo 429 rate limits.
- T3: Eliminate O(n²) disk writes + lock contention during symbol resolution by batching the cache save into a single write per enrichment pass.
- T4: Fix a race condition where /api/refresh-prices runs enrichment without holding _operation_lock, conflicting with the daily enrichment loop.

Output: Three atomic commits modifying app/market_data.py (T2, T3) and app/main.py (T4).

Scope guard: Do NOT implement T1 (already done, commit cc35de8), T5, T6, or T7. Do NOT touch _post_enrich_one, _enrich_one, enrich_position, or _yf_throttle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.hermes/plans/enrichment-optimization.md

<interfaces>
<!-- Key existing contracts the executor must build against. Already extracted — no exploration needed. -->

app/market_data.py module-level (lines 31-33):
```python
_resolution_cache: dict[str, dict] = {}
_resolution_cache_lock = threading.RLock()
_SYMBOL_CACHE_PATH = "/data/snapshots/symbol_cache.json"
```

app/market_data.py `_save_symbol_cache` (lines 233-250) — already writes atomically via tmp + os.replace; serialized by _SYMBOL_CACHE_FILE_LOCK and _resolution_cache_lock. Signature: `def _save_symbol_cache() -> None`. Called internally from lines 513, 558, 566, 685, 1110, 1301, 1513 (inside _resolve_yf_symbol / _resolve_by_isin / others) AND directly from the stage 2 loop at line 1658.

app/market_data.py `enrich_positions` stage 2 resolution loop: lines 1620-1669 (the `for idx, pos in enumerate(positions)` loop that resolves yf symbols). The in-loop direct save is at line 1658.

app/market_data.py batch fetch helper `_fetch_and_unpack`: lines 1683-1709, called at line 1711 as `price_batch = {**_fetch_and_unpack(eu_symbols), **_fetch_and_unpack(us_symbols)}`. The download call (line 1688):
```python
b = yf.download(symbols, period="2d", auto_adjust=True, progress=False, threads=False)
```

app/main.py operation lock (lines 71-89):
```python
_operation_lock = asyncio.Lock()
_operation_lock_held = False
async def _acquire_operation_lock():  # returns True if acquired, False if already held
async def _is_operation_locked():     # returns _operation_lock_held
def _release_operation_lock(): ...
```

app/main.py `_do_enrich_session_async` (lines 871-881):
```python
async def _do_enrich_session_async():
    if not await _acquire_operation_lock():
        raise HTTPException(status_code=409, detail="Another operation is already running")
    try:
        await asyncio.to_thread(_do_enrich_session)
    finally:
        _release_operation_lock()
```

app/main.py `refresh_prices` (lines 884-898) — current racy implementation:
```python
@app.post("/api/refresh-prices", dependencies=[Depends(verify_brok_token)], response_model=dict)
async def refresh_prices():
    with _sync_lock:
        portfolio = _session.get("portfolio")
    if not portfolio or not portfolio.get("positions"):
        raise HTTPException(status_code=400, detail="No portfolio loaded")
    thread = threading.Thread(target=_do_enrich_session, daemon=True)
    thread.start()
    return {"status": "enrichment_started"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>T2: threads=True + chunking in yf.download batch price fetch</name>
  <files>app/market_data.py</files>
  <behavior>
    - A symbol group of <=25 symbols downloads in a single yf.download call with threads=True; results are identical in shape to before.
    - A symbol group of >25 symbols is split into consecutive chunks of at most 25, each downloaded separately with threads=True, and the per-chunk result dicts are merged into one dict.
    - The EU/US split at lines 1680-1681 is preserved; chunking happens inside _fetch_and_unpack per group, AFTER the EU/US split (an EU group of 60 -> 25+25+10, US group handled independently).
    - A failure in one chunk (exception) is logged and skipped; other chunks still contribute their prices (the existing try/except already isolates failures — keep that behavior per-chunk).
  </behavior>
  <action>
    Modify ONLY the `_fetch_and_unpack` helper inside `enrich_positions` (lines 1683-1709) and nothing else.

    1. Change the download call (line 1688) from `threads=False` to `threads=True`.
    2. Add chunking. Define a constant `_CHUNK_SIZE = 25` (local to the helper or as a module-level constant near the top of the function — your discretion, but keep it visible). Refactor `_fetch_and_unpack` so it splits `symbols` into chunks of at most 25 (`[symbols[i:i+25] for i in range(0, len(symbols), 25)]`) and runs the existing download + unpack logic once per chunk, merging each chunk's results into the single `result` dict it returns.
    3. Keep the per-chunk download wrapped in the existing try/except so a 429 or error on one chunk does not abort the others — preserve the `logger.warning("Batch price fetch failed for %s: %s", symbols, e)` message (it may now report the failing chunk's symbols).
    4. Do NOT change the EU/US split (lines 1680-1681), the call site (line 1711), the Close-DataFrame/Series unpack logic, or the `price > 0` filter — only thread the chunking around the existing download+unpack body.

    Per source plan T2: yfinance handles threading internally; chunking at ~25 avoids Yahoo 429 rate limits. Suggested commit message: `perf(enrichment): enable threads=True in yf.download with chunking (T2)`.

    Commit this change atomically before starting T3.
  </action>
  <verify>
    <automated>cd /home/server/workspace/brokr && grep -n "threads=True" app/market_data.py | grep -q "." && grep -n "range(0, len" app/market_data.py</automated>
  </verify>
  <done>_fetch_and_unpack uses threads=True, splits any symbol list >25 into chunks of 25, merges results; EU/US split and unpack logic unchanged; committed as its own commit.</done>
</task>

<task type="auto" tdd="true">
  <name>T3: Batch symbol cache disk writes in stage 2 (deferred save)</name>
  <files>app/market_data.py</files>
  <behavior>
    - During the stage 2 resolution loop (lines 1620-1669), no per-symbol disk write to symbol_cache.json occurs: every `_save_symbol_cache()` call triggered directly (line 1658) or indirectly via `_resolve_yf_symbol`/`_resolve_by_isin` (lines 513, 558, 566, 685, 1301) is a no-op for disk persistence.
    - The in-memory `_resolution_cache` dict is still updated under `_resolution_cache_lock` exactly as before (source of truth unchanged).
    - Exactly ONE `_save_symbol_cache()` disk write happens at the end of the stage 2 loop, persisting all accumulated changes — but ONLY if at least one resolution actually requested a save during the window (avoid an unconditional write when nothing changed).
    - Outside the deferral window (e.g. the cache-clear endpoint at 1513, or other callers), `_save_symbol_cache()` writes to disk immediately as today — behavior unchanged for all callers not inside the stage 2 loop.
    - If the process is interrupted mid-loop, the on-disk JSON is never corrupted (atomic tmp+os.replace already guarantees this); at worst, unsaved in-memory entries are re-resolved next run.
  </behavior>
  <action>
    Use a thread-local defer flag (mechanism option (a) from the task brief) — safest because it makes the many internal `_save_symbol_cache()` callers no-ops without changing their signatures.

    1. Near the cache definitions (around lines 31-33), add a module-level `_save_defer = threading.local()` thread-local.
    2. Modify `_save_symbol_cache()` (lines 233-250): at the top, check `getattr(_save_defer, "active", False)`. If active, set `_save_defer.dirty = True` and return immediately WITHOUT writing to disk. Otherwise proceed with the existing atomic write. This makes deferral thread-safe and re-entrancy-safe — only the thread running stage 2 defers; other threads still write normally.
    3. Wrap the stage 2 resolution loop in `enrich_positions` (the `for idx, pos in enumerate(positions)` block, lines 1620-1669) so that before the loop you set `_save_defer.active = True` and `_save_defer.dirty = False`, and AFTER the loop (in a `finally`) you set `_save_defer.active = False` and, if `_save_defer.dirty` is True, call `_save_symbol_cache()` exactly once to flush. Use try/finally so the flag is always cleared even if resolution raises.
    4. Do NOT remove the existing in-loop `_save_symbol_cache()` call at line 1658 or the internal calls inside the resolve functions — they simply become no-ops during the window and the in-memory dict updates still run. (Removing them would break the immediate-write behavior when those functions are called outside enrich_positions.)
    5. Do NOT change `_resolution_cache_lock` usage, the atomic tmp+os.replace logic, or any resolve-function signatures.

    Per source plan T3: in-memory `_resolution_cache` is the source of truth (written under lock regardless); only DISK persistence is batched. Suggested commit message: `perf(enrichment): batch symbol cache disk writes in stage 2 (T3)`.

    Commit this change atomically before starting T4.
  </action>
  <verify>
    <automated>cd /home/server/workspace/brokr && grep -n "_save_defer" app/market_data.py | grep -vc '^#' | grep -qv '^0$' && grep -n "threading.local()" app/market_data.py</automated>
  </verify>
  <done>_save_symbol_cache is a no-op for disk when _save_defer.active; stage 2 loop sets/clears the flag in try/finally and flushes once if dirty; in-memory dict updates and internal save calls unchanged; non-stage-2 callers still write immediately; committed as its own commit.</done>
</task>

<task type="auto">
  <name>T4: Acquire operation lock in /api/refresh-prices</name>
  <files>app/main.py</files>
  <action>
    Fix the race in `refresh_prices` (lines 884-898): it launches `_do_enrich_session` in a raw `threading.Thread` without holding `_operation_lock` (an `asyncio.Lock`), racing the daily loop. The lock CANNOT be acquired from a raw thread.

    Resolution: replace the raw `threading.Thread` with a fire-and-forget `asyncio.create_task(_do_enrich_session_async())` scheduled on the running event loop. `_do_enrich_session_async` already acquires `_operation_lock` before offloading `_do_enrich_session` via `asyncio.to_thread`, so this serializes against the daily loop while preserving the non-blocking "return immediately" contract.

    Handle the 409 lock-contention case for a background task (the HTTP response has already returned, so it cannot surface 409 to the client):
    1. Keep the existing early `400 No portfolio loaded` guard (lines 891-894) unchanged.
    2. Replace lines 896-897 (`thread = threading.Thread(...)` + `thread.start()`) with creating a background task that runs `_do_enrich_session_async()`. Because `_do_enrich_session_async` raises HTTPException(409) when the lock is already held, wrap the call in a small async coroutine that catches that contention case and logs a warning (e.g. "refresh-prices skipped: another operation already running") instead of letting an unhandled exception escape the background task. Define this as a local `async def _runner(): ...` inside the endpoint (or an equivalent inline coroutine) and schedule it with `asyncio.create_task(_runner())`.
    3. Preserve the immediate response `return {"status": "enrichment_started"}` exactly.
    4. Ensure `asyncio` is imported in main.py (it is already used at line 879 via `asyncio.to_thread`, so the import exists — confirm, do not duplicate).
    5. Do NOT modify `_do_enrich_session`, `_do_enrich_session_async`, `_acquire_operation_lock`, or `_release_operation_lock`.

    Per source plan T4. Suggested commit message: `fix(enrichment): acquire operation lock in /api/refresh-prices to prevent race (T4)`.

    Commit this change atomically.
  </action>
  <verify>
    <automated>cd /home/server/workspace/brokr && grep -n "create_task" app/main.py | grep -q "." && grep -n "threading.Thread(target=_do_enrich_session" app/main.py; test $? -ne 0</automated>
  </verify>
  <done>refresh_prices schedules _do_enrich_session_async via asyncio.create_task (lock acquired before the to_thread offload), catches/logs the 409 contention case in a background runner, preserves the 400 guard and the immediate {"status": "enrichment_started"} response, and no longer uses a raw threading.Thread for enrichment; committed as its own commit.</done>
</task>

</tasks>

<verification>
After all three commits, rebuild the dev container and run the test suite:

```bash
docker compose -f docker-compose.dev.yml up -d --build
docker exec brokr bash -c "cd /app && python -m pytest tests/ -q"
```

(Use docker-compose.dev.yml per project memory.) If the container or tests cannot run in this environment, the executor still commits all three code changes and records in the SUMMARY that container-based verification was not run, plus the exact command above for the user to run.

Static verification that must pass regardless of container availability:
- `grep -n "threads=True" app/market_data.py` returns a match (T2)
- `grep -n "range(0, len" app/market_data.py` shows chunking (T2)
- `grep -n "_save_defer" app/market_data.py` returns matches (T3)
- `grep -n "create_task" app/main.py` returns a match and the old `threading.Thread(target=_do_enrich_session` is gone (T4)
</verification>

<success_criteria>
- T2: _fetch_and_unpack uses threads=True and chunks >25-symbol groups into batches of 25, merging results. Only the batch price fetch stage changed.
- T3: Stage 2 resolution loop performs a single disk write of the symbol cache via a thread-local defer flag; in-memory cache and all resolve-function behavior unchanged; non-stage-2 callers still write immediately.
- T4: /api/refresh-prices acquires _operation_lock via asyncio.create_task(_do_enrich_session_async()), handles 409 contention gracefully in the background, and returns {"status": "enrichment_started"} immediately.
- Three separate atomic commits with the suggested messages.
- T1/T5/T6/T7 untouched; _post_enrich_one/_enrich_one/enrich_position/_yf_throttle untouched.
</success_criteria>

<output>
Create `.planning/quick/260529-lm1-implement-enrichment-pipeline-optimizati/260529-lm1-SUMMARY.md` when done.
</output>
