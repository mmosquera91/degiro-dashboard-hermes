---
name: "20260426-isin-direct-lookup-tradegate"
description: "Add Step 0.5 ISIN direct lookup for Tradegate US stocks"
type: "quick"
date: "2026-04-26"
status: "complete"
---

## Summary

**Completed:** 2026-04-26

Added Step 0.5 to `_resolve_yf_symbol()` in `app/market_data.py` — a direct ISIN lookup for Tradegate (exchange_id=196) US stocks. The change inserts between the exchangeId candidate step and the manual overrides step.

**File changed:** `app/market_data.py` (lines 444–467)

**What it does:** When exchange_id is "196" and the ISIN starts with "US", yfinance is called directly with the ISIN string (yf.Ticker(isin)). If history data is returned, the canonical ticker is extracted from `t.info["symbol"]` and cached. Falls through gracefully if lookup fails.

**Verification:**
- `python3 -m py_compile app/market_data.py` — syntax OK
- No other source files modified
- symbol_overrides.json has no NVDA/PLTR/APP/ARM entries (clean — no stale overrides)

**Next step after deployment:** Clear symbol cache so NVD/PTX/6RV/O9T re-resolve via ISIN lookup → NVDA/PLTR/APP/ARM.
