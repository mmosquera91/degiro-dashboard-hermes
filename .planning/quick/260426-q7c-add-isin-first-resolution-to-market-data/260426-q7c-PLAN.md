---
name: 260426-q7c-add-isin-first-resolution-to-market-data
description: Add ISIN-first resolution to market_data.py for ETFs/ETPs whose DeGiro symbol doesn't match Yahoo ticker
date: 2026-04-26
status: complete
quick_id: 260426-q7c
slug: add-isin-first-resolution-to-market-data

## Plan

**Goal:** Add ISIN-based Yahoo Finance ticker resolution as the first step in `_resolve_yf_symbol()`, before the suffix scan. This fixes ETF/ETP instruments (QDVD, QDV5, O9T, etc.) whose DeGiro symbol doesn't match their Yahoo ticker.

**must_haves:**
- `_resolve_by_isin()` function using `yfinance.Search(isin)` that prefers currency-matched exchanges
- `_resolve_yf_symbol()` updated to call ISIN resolution before suffix scan
- `position_currency` parameter propagated from `enrich_position()` → `_resolve_yf_symbol()` → `_resolve_by_isin()`
- Rate limit handling integrated with existing `_yf_rate_limited` global

**Tasks:**
1. Add `_resolve_by_isin()` function after `_save_symbol_cache()` — uses `yf.Search(isin)` with EUR/USD/GBP exchange preference
2. Update `_resolve_yf_symbol()` signature to accept `position_currency` parameter
3. Insert ISIN resolution as Step 0 before the suffix loop
4. Update `enrich_position()` caller to pass `position.get("currency", "EUR")`

**verification:**
- Manual: After deploy, call `DELETE /api/admin/symbol-cache` to evict bad cache entries for QDVD, QDV5, O9T etc.
- Instruments with ISINs should resolve via Yahoo Finance search before suffix scan runs

**done:** Code changes implemented and committed