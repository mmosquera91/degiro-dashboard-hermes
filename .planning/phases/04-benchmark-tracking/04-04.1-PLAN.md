---
phase: 04-benchmark-tracking
plan: "04.1"
type: gap_closure
wave: 1
depends_on: []
files_modified:
  - app/snapshots.py
autonomous: true
gap_closure: true
gaps_addressed:
  - "CR-01: sign inversion in compute_attribution() — code is fixed, stale comment needs updating"
  - "WR-01: frontend Math.abs() sorting — already fixed in codebase"
  - "WR-02: date range validation — already fixed in codebase"
  - "IN-01: CSS var(----border) typo — already fixed in commit a185485"

must_haves:
  truths:
    - "compute_attribution() correctly produces negative relative_contribution for losing positions"
    - "Frontend correctly sorts by raw absolute_contribution (no Math.abs)"
    - "fetch_benchmark_series validates date range before yfinance call"
    - "CSS border properties use valid custom property syntax"
    - "Comment in compute_attribution() accurately reflects the implemented formula"
  artifacts:
    - path: app/snapshots.py
      provides: "compute_attribution comment matches actual formula (no stale mention of direction)"
      contains: "# relative_contribution = (position_return - benchmark_return) * weight  # D-11"
---

<objective>
Close gaps identified in 04-VERIFICATION.md. CR-01 code is already fixed (lines 162-165 have correct formula). WR-01, WR-02, IN-01 are already fixed in the codebase. The only remaining task is updating a stale comment in compute_attribution() docstring that still describes the old buggy formula.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@app/snapshots.py (compute_attribution docstring at lines 138-154)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stale docstring comment in compute_attribution()</name>
  <files>app/snapshots.py</files>
  <read_first>
app/snapshots.py lines 138-175
</read_first>
  <action>
The docstring comment at lines 141-145 in compute_attribution() is stale — it still describes the old buggy formula that included `* direction`:

Current stale comment (lines 141-145):
```
Attribution formula (D-11):
    relative_contribution = (position_return - benchmark_return) * weight * direction
    absolute_contribution = position_return * weight
    direction = 1 if position_return >= 0 else -1
    weight = position weight as decimal (e.g., 0.20 for 20%)
```

Replace with the correct formula matching the actual implementation at lines 162-165:
```
Attribution formula (D-11):
    relative_contribution = (position_return - benchmark_return) * weight
    absolute_contribution = position_return * weight
    weight = position weight as decimal (e.g., 0.20 for 20%)
```

The `direction` variable no longer exists in the code — the fix removes it entirely.
</action>
  <acceptance_criteria>
- grep "direction = 1 if position_return >= 0 else -1" app/snapshots.py returns empty (stale comment removed)
- grep "relative_contribution = (position_return - benchmark_return) \* weight \* direction" app/snapshots.py returns empty
- python3 -c "from app.snapshots import compute_attribution; r = compute_attribution([{'name':'T','symbol':'T','perf_ytd':-10.0,'weight':10.0}], 0.0); assert r[0]['relative_contribution'] == -1.0, f'got {r[0][\"relative_contribution\"]}'; print('PASS')" returns PASS
</acceptance_criteria>
  <verify>
<automated>
python3 -c "
from app.snapshots import compute_attribution
# CR-01 verification: losing position
r = compute_attribution([{'name':'T','symbol':'T','perf_ytd':-10.0,'weight':10.0}], 0.0)
assert r[0]['relative_contribution'] == -1.0, f'CR-01 FAIL: got {r[0][\"relative_contribution\"]}'
print('CR-01: PASS (relative_contribution=-1.0 for -10% loss)')
# CR-01 verification: winning position
r2 = compute_attribution([{'name':'W','symbol':'W','perf_ytd':10.0,'weight':10.0}], 0.0)
assert r2[0]['relative_contribution'] == 1.0, f'CR-01 FAIL: got {r2[0][\"relative_contribution\"]}'
print('CR-01 win: PASS (relative_contribution=1.0 for +10% gain)')
# Verify stale comment is gone
import subprocess
result = subprocess.run(['grep', '-n', 'direction = 1 if position_return >= 0 else -1', 'app/snapshots.py'], capture_output=True, text=True)
assert result.returncode != 0, 'Stale comment still present'
print('Stale comment: REMOVED')
"
</automated>
  </verify>
  <done>Stale docstring comment updated; compute_attribution formula documented correctly</done>
</task>

</tasks>

<verification>
After this plan:
- CR-01: `compute_attribution()` returns -1.0 for -10% loss (verified by Python test)
- Stale `direction` comment removed from docstring
- WR-01: `grep 'Math.abs' app/static/app.js` returns empty (already fixed)
- WR-02: `grep 'start_dt >= end_dt' app/snapshots.py` returns validation line (already fixed)
- IN-01: `grep '----border' app/static/style.css` returns empty (already fixed by a185485)
</verification>

<success_criteria>
1. `compute_attribution([{'perf_ytd': -10.0, 'weight': 10.0}], 0.0)` returns `relative_contribution = -1.0`
2. `grep 'direction = 1 if position_return >= 0' app/snapshots.py` returns empty (stale comment removed)
3. `grep 'Math.abs' app/static/app.js` returns empty (WR-01 already fixed)
4. `grep '----border' app/static/style.css` returns empty (IN-01 already fixed)
</success_criteria>

<output>
After completion, create `.planning/phases/04-benchmark-tracking/04-04.1-SUMMARY.md`
</output>