---
phase: "06"
plan: "03"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/test_market_data.py
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "get_fx_rate returns 1.0 for same-currency"
    - "get_fx_rate returns cached value on cache hit"
    - "get_fx_rate falls back to 1.0 when yfinance lookup fails"
    - "get_fx_rate tries inverse when direct lookup fails"
    - "compute_rsi returns correct value for known input"
    - "compute_rsi returns None when insufficient data"
    - "enrich_position returns position with all yfinance fields set"
    - "enrich_position handles missing symbol gracefully"
    - "enrich_position handles yfinance failure gracefully"
  artifacts:
    - path: "tests/test_market_data.py"
      provides: "Unit tests for market_data.py functions"
      min_lines: 100
      contains:
        - "def test_get_fx_rate_same_currency"
        - "def test_get_fx_rate_cache_hit"
        - "def test_get_fx_rate_yf_failure"
        - "def test_compute_rsi_happy_path"
        - "def test_compute_rsi_insufficient_data"
        - "def test_enrich_position_happy_path"
        - "def test_enrich_position_no_symbol"
        - "def test_enrich_position_yf_failure"
---

<objective>
Create unit tests for `market_data.py` covering get_fx_rate, compute_rsi, and enrich_position as required by TEST-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/market_data.py
tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/test_market_data.py</name>
  <files>tests/test_market_data.py</files>
  <read_first>app/market_data.py</read_first>
  <action>
Create `tests/test_market_data.py` with pytest tests for `get_fx_rate`, `compute_rsi`, and `enrich_position`. All external calls must be mocked with `unittest.mock.patch`.

**Test `get_fx_rate`:**

1. **test_get_fx_rate_same_currency** — EUR->EUR returns 1.0 without cache lookup:
```python
def test_get_fx_rate_same_currency():
    from market_data import get_fx_rate
    with patch("market_data.yf.Ticker") as mock_ticker:
        result = get_fx_rate("EUR", "EUR")
        assert result == 1.0
        mock_ticker.assert_not_called()  # no yfinance call for same currency
```

2. **test_get_fx_rate_cache_hit** — second call returns cached value:
```python
def test_get_fx_rate_cache_hit(fx_rate_cache):
    from market_data import get_fx_rate
    fx_rate_cache["USDEUR"] = 0.92  # seed cache
    with patch("market_data.yf.Ticker") as mock_ticker:
        result = get_fx_rate("USD", "EUR")
        assert result == 0.92
        mock_ticker.assert_not_called()  # no yfinance call, cache hit
```

3. **test_get_fx_rate_yf_failure** — yfinance fails, returns 1.0 fallback:
```python
def test_get_fx_rate_yf_failure(fx_rate_cache):
    from market_data import get_fx_rate
    with patch("market_data.yf.Ticker") as mock_ticker:
        mock_instance = MagicMock()
        mock_instance.history.return_value.empty = True  # no data
        mock_ticker.return_value = mock_instance
        result = get_fx_rate("USD", "EUR")
        assert result == 1.0  # fallback
```

4. **test_get_fx_rate_direct_lookup** — yfinance returns valid rate:
```python
def test_get_fx_rate_direct_lookup(fx_rate_cache):
    from market_data import get_fx_rate
    with patch("market_data.yf.Ticker") as mock_ticker:
        mock_instance = MagicMock()
        mock_hist = MagicMock()
        mock_hist.empty = False
        mock_hist.__getitem__ = MagicMock(return_value=MagicMock(iloc=[MagicMock()]))
        mock_hist.iloc = [MagicMock(), MagicMock()]
        mock_hist.iloc[-1] = 0.92
        mock_instance.history.return_value = mock_hist
        mock_ticker.return_value = mock_instance
        result = get_fx_rate("USD", "EUR")
        assert result == 0.92
```

**Test `compute_rsi`:**

5. **test_compute_rsi_happy_path** — verify RSI calculation with known input:
```python
def test_compute_rsi_happy_path():
    from market_data import compute_rsi
    import pandas as pd
    # Create a price series with known RSI behavior
    # Simple uptrend should give RSI > 50
    prices = pd.Series([100, 102, 101, 103, 102, 104, 103, 105, 104, 106, 105, 107, 106, 108, 107])
    result = compute_rsi(prices, period=14)
    assert result is not None
    assert isinstance(result, float)
    assert 0 <= result <= 100
```

6. **test_compute_rsi_insufficient_data** — less than period+1 data returns None:
```python
def test_compute_rsi_insufficient_data():
    from market_data import compute_rsi
    import pandas as pd
    # Fewer than 15 data points (14 period + 1)
    prices = pd.Series([100, 101, 102])
    result = compute_rsi(prices, period=14)
    assert result is None
```

7. **test_compute_rsi_no_losses** — all gains returns RSI 100:
```python
def test_compute_rsi_no_losses():
    from market_data import compute_rsi
    import pandas as pd
    # Monotonically increasing prices -> no losses -> RSI 100
    prices = pd.Series([100, 101, 102, 103, 104, 105, 106, 107, 108, 109, 110, 111, 112, 113, 114])
    result = compute_rsi(prices, period=14)
    assert result == 100.0
```

**Test `enrich_position`:**

8. **test_enrich_position_happy_path** — enriches with yfinance data:
```python
def test_enrich_position_happy_path(mock_yfinance_ticker):
    from market_data import enrich_position
    import pandas as pd

    pos = {
        "symbol": "AAPL",
        "isin": "US0378331005",
        "name": "Apple Inc",
        "asset_type": "STOCK",
        "quantity": 10.0,
        "avg_buy_price": 150.0,
    }

    with patch("market_data.yf.Ticker", return_value=mock_yfinance_ticker):
        with patch("market_data.compute_rsi") as mock_rsi:
            mock_rsi.return_value = 55.0
            result = enrich_position(pos)

    assert result["sector"] == "Technology"
    assert result["country"] == "United States"
    assert result["52w_high"] is not None
    assert result["52w_low"] is not None
    assert result["rsi"] == 55.0
    assert result["perf_30d"] is not None or result["perf_30d"] is None  # depends on history length
```

9. **test_enrich_position_no_symbol** — missing symbol returns position unchanged:
```python
def test_enrich_position_no_symbol():
    from market_data import enrich_position
    pos = {"name": "Unknown", "quantity": 5.0}
    with patch("market_data.logger") as mock_logger:
        result = enrich_position(pos)
    assert result["name"] == "Unknown"
    mock_logger.warning.assert_called()
```

10. **test_enrich_position_yf_failure** — yfinance failure sets fields to None gracefully:
```python
def test_enrich_position_yf_failure():
    from market_data import enrich_position
    pos = {"symbol": "FAIL", "isin": "XX", "name": "Fail Corp", "quantity": 1.0}
    with patch("market_data.yf.Ticker") as mock_ticker:
        mock_instance = MagicMock()
        mock_instance.info = {}  # empty info
        mock_instance.history.side_effect = Exception("Network error")
        mock_ticker.return_value = mock_instance
        result = enrich_position(pos)
    # All yfinance-dependent fields should be None
    assert result["rsi"] is None
    assert result["perf_30d"] is None
    assert result["perf_90d"] is None
    assert result["perf_ytd"] is None
```

The file must import from `market_data`, use `pytest`, and `unittest.mock.patch`.
</action>
  <verify>
    <automated>cd /home/server/workspace/brokr && python -m pytest tests/test_market_data.py -v --tb=short 2>&1 | head -80</automated>
  </verify>
  <done>
    tests/test_market_data.py exists with 10+ test functions covering:
    - get_fx_rate: same currency, cache hit, yfinance failure, direct lookup
    - compute_rsi: happy path, insufficient data, no losses
    - enrich_position: happy path, no symbol, yfinance failure
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {tests -> market_data.py} | Test code imports app/market_data.py; yfinance is mocked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Denial | test_market_data.py | mitigate | Tests mock all external calls; no live network used |
</threat_model>

<verification>
- pytest tests/test_market_data.py -v passes all tests
- All 10 test functions are present
</verification>

<success_criteria>
- test_market_data.py covers all functions in TEST-02: get_fx_rate, compute_rsi, enrich_position
- Tests mock yfinance completely (no live data, no network calls)
- All tests pass with pytest
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing/06-03-SUMMARY.md`
</output>
