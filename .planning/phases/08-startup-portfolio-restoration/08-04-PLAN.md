---
phase: "08"
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/main.py
autonomous: true
gap_closure: true
source_gap: "08-VERIFICATION.md"
requirements:
  - REST-01
  - REST-02
  - REST-03
must_haves:
  truths:
    - "Portfolio is restored from snapshot on startup when snapshot exists"
    - "get_portfolio() serves cached portfolio without session check"
  artifacts:
    - path: "app/main.py:8"
      provides: "Path imported from pathlib"
      contains: "from pathlib import Path"
---

<objective>
Fix a critical bug from phase 08 verification: `app/main.py` uses `Path` at line 229 but does not import it. This causes `_restore_portfolio_from_snapshot()` to crash with `NameError: name 'Path' is not defined` when a valid snapshot exists, preventing portfolio restoration entirely.

Fix: Add `from pathlib import Path` to the imports in `app/main.py` (around line 8).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add missing Path import to app/main.py</name>
  <files>app/main.py</files>
  <action>
Read the imports section of app/main.py (lines 1-30). Find the appropriate location and add:
```python
from pathlib import Path
```

The import should be added alongside other standard library imports (around line 8, after `import json`).
</action>
  <verify>
    <automated>grep -n "from pathlib import Path" app/main.py</automated>
  </verify>
  <done>Path is imported in app/main.py</done>
</task>

</tasks>

<verification>
## Phase-Level Verification

1. **Path import present:**
   - `grep -n "from pathlib import Path" app/main.py` returns the import line

2. **Restore succeeds with snapshot present:**
   - `_restore_portfolio_from_snapshot()` no longer raises NameError when a snapshot exists
   - `_session["portfolio"]` is populated after restore

3. **All prior truths now pass:**
   - Truth 1: After container restart, GET /api/portfolio returns last-known portfolio without 401
   - Truth 2: No DeGiro session is required to serve the restored portfolio
</verification>

<success_criteria>
- [ ] `from pathlib import Path` added to app/main.py imports
- [ ] `_restore_portfolio_from_snapshot()` no longer crashes when snapshot exists
- [ ] `_session["portfolio"]` is populated after successful restore
</success_criteria>

<output>
After completion, create `.planning/phases/08-startup-portfolio-restoration/08-04-SUMMARY.md`
</output>
