---
phase: "08"
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/main.py
autonomous: true
gap_closure: true
source_gap: "08-UAT.md"
requirements:
  - REST-01
  - REST-02
  - REST-03
must_haves:
  truths:
    - "Portfolio is restored from snapshot on startup when snapshot exists"
    - "get_portfolio() serves cached portfolio without session check"
  artifacts:
    - path: "app/main.py:248-260"
      provides: "lifespan.__aenter__() calls _restore_portfolio_from_snapshot()"
      contains: "_restore_portfolio_from_snapshot"
---

<objective>
Fix critical startup sequencing bug: `_restore_portfolio_from_snapshot()` is only called from the deprecated `@app.on_event("startup")` handler, which does not fire reliably in FastAPI 0.100+ when a `lifespan` context manager is also defined. Move the restore call into `lifespan.__aenter__()` so it runs before the API server accepts requests.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Move _restore_portfolio_from_snapshot() into lifespan.__aenter__()</name>
  <files>app/main.py</files>
  <action>
Read `app/main.py` — find the `lifespan()` context manager (lines ~248-260) and `on_startup()` function (lines ~264-289).

**In `lifespan()` `__aenter__()` section (after `logger.info("Brokr starting up")`):**

Add a call to `_restore_portfolio_from_snapshot()`:
```python
# REST-01: Restore portfolio from latest snapshot
_restore_portfolio_from_snapshot()
```

**In `on_startup()` function:**
Remove the call to `_restore_portfolio_from_snapshot()` at line 289 (the line should still log "Startup event fired" but should not call the restore function). Keep the DNS check and module imports in `on_startup()` for now since those are separate concerns.

The `on_startup()` function should become a no-op for snapshot restore since that logic is now in lifespan.
</action>
  <verify>
    <automated>grep -n "_restore_portfolio_from_snapshot" app/main.py</automated>
  </verify>
  <done>lifespan.__aenter__() calls _restore_portfolio_from_snapshot() before yield; on_startup() does not call it</done>
</task>

</tasks>

<verification>
## Phase-Level Verification

1. **lifespan calls restore:**
   - `grep -n "_restore_portfolio_from_snapshot" app/main.py` shows the call inside `lifespan.__aenter__()` section

2. **on_startup does not call restore:**
   - The line in `on_startup()` that calls `_restore_portfolio_from_snapshot()` is removed or commented

3. **UAT tests pass:**
   - Cold Start Smoke Test: portfolio returned from snapshot after server restart
   - Portfolio Served After Restart Without DeGiro Session: returns portfolio data, not 401
</verification>

<success_criteria>
- [ ] `lifespan.__aenter__()` contains `_restore_portfolio_from_snapshot()` call
- [ ] `on_startup()` does NOT call `_restore_portfolio_from_snapshot()`
- [ ] UAT tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-startup-portfolio-restoration/08-05-SUMMARY.md`
</output>
